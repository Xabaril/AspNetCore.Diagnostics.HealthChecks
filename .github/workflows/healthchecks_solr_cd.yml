name: HealthChecks Solr CD

on:
  push:
    tags:
      - release-solr-*
      - release-all-*

jobs:
  build:
    uses: ./.github/workflows/reusable_cd_workflow.yml
    secrets: inherit
    with:
      BUILD_CONFIG: Release
      PROJECT_PATH: ./src/HealthChecks.Solr/HealthChecks.Solr.csproj
      PACKAGE_NAME: AspNetCore.HealthChecks.Solr
